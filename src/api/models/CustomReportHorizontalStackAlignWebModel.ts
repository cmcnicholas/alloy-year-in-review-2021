/* eslint-disable */

/**
 * Defines how to align the controls inside a horizontal stack control. This only applies to direct descendants,
 * it does not cascade further
 */
export enum CustomReportHorizontalStackAlignWebModel {
  Left = 'Left',
  Right = 'Right',
  Centre = 'Centre',
}
