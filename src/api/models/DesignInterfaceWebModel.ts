/* eslint-disable */

import type { DodiWebModel } from './DodiWebModel';

export type DesignInterfaceWebModel = DodiWebModel;

export namespace DesignInterfaceWebModel {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'DesignInterfaceWebModel';
}
