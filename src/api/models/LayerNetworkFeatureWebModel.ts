/* eslint-disable */

/**
 * Web model for a layer network GeoJson feature. It represents an entity to show on the map
 */
export type LayerNetworkFeatureWebModel = {};

export namespace LayerNetworkFeatureWebModel {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'LayerNetworkFeatureWebModel';
}
