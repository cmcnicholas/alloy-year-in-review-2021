/* eslint-disable */

/**
 * Web model for a layer Basic GeoJson feature.
 */
export type LayerBasicFeatureWebModel = {};

export namespace LayerBasicFeatureWebModel {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'LayerBasicFeatureWebModel';
}
