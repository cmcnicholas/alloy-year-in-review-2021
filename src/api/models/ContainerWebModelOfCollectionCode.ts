/* eslint-disable */

import type { CollectionCode } from './CollectionCode';

/**
 * Optional web model
 */
export type ContainerWebModelOfCollectionCode = {
  /**
   * The value of this property
   */
  value?: CollectionCode;
};

export namespace ContainerWebModelOfCollectionCode {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'ContainerWebModelOfCollectionCode';
}
