/* eslint-disable */

/**
 * Base class for applicable from types
 */
export type ApplicableFromWebModelBase = {
  discriminator: string;
};

export namespace ApplicableFromWebModelBase {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'ApplicableFromWebModelBase';
}
