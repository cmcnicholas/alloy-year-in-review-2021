/* eslint-disable */

/**
 * Web model for a layer cluster GeoJson feature. It represents an entity to show on the map
 */
export type LayerClusterFeatureWebModel = {};

export namespace LayerClusterFeatureWebModel {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'LayerClusterFeatureWebModel';
}
