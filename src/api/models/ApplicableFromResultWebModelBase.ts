/* eslint-disable */

/**
 * Base class for applicable types from
 */
export type ApplicableFromResultWebModelBase = {
  discriminator: string;
};

export namespace ApplicableFromResultWebModelBase {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'ApplicableFromResultWebModelBase';
}
