/* eslint-disable */

export type JObject = {};

export namespace JObject {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'JObject';
}
