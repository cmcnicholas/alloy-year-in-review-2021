/* eslint-disable */

/**
 * Base class for syntax values that select a value from an item
 */
export type WorkflowSyntaxArgumentItemValueBaseWebModel = {
  discriminator: string;
};

export namespace WorkflowSyntaxArgumentItemValueBaseWebModel {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'WorkflowSyntaxArgumentItemValueBaseWebModel';
}
