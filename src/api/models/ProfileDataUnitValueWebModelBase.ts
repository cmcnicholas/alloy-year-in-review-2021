/* eslint-disable */

/**
 * Base class for the value of an element of profile data
 */
export type ProfileDataUnitValueWebModelBase = {
  discriminator: string;
};

export namespace ProfileDataUnitValueWebModelBase {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'ProfileDataUnitValueWebModelBase';
}
