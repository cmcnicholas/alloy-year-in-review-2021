/* eslint-disable */

import type { ExportWebRequestModelBase } from './ExportWebRequestModelBase';

export type ShapefileExportWebRequestModel = ExportWebRequestModelBase;

export namespace ShapefileExportWebRequestModel {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'ShapefileExportWebRequestModel';
}
