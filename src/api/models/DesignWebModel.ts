/* eslint-disable */

import type { DodiWebModel } from './DodiWebModel';

export type DesignWebModel = DodiWebModel;

export namespace DesignWebModel {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'DesignWebModel';
}
