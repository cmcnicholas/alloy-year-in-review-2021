/* eslint-disable */

/**
 * Base web model for dodi attribute options
 */
export type DodiAttributeOptionsWebModelBase = {
  discriminator: string;
};

export namespace DodiAttributeOptionsWebModelBase {
  /**
   * the discriminator for the model if required for more complex api's
   */
  export const discriminator = 'DodiAttributeOptionsWebModelBase';
}
